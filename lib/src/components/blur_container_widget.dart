import 'package:flutter/material.dart';

class BlurContainerWidget extends StatelessWidget {
  const BlurContainerWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Row(
        children: [],
      ),
    );
  }
}
